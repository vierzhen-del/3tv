{
  "name": "3protv 오늘 종합 다이제스트 → 텔레그램",
  "settings": {
    "timezone": "Asia/Seoul",
    "executionOrder": "v1"
  },
  "nodes": [
    {
      "id": "schedule",
      "name": "평일 18:00",
      "type": "n8n-nodes-base.scheduleTrigger",
      "typeVersion": 1.2,
      "position": [0, 0],
      "parameters": {
        "rule": {
          "interval": [
            {
              "field": "cronExpression",
              "expression": "0 18 * * 1-5"
            }
          ]
        }
      }
    },
    {
      "id": "calcpath",
      "name": "오늘 경로 계산",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [200, 0],
      "parameters": {
        "jsCode": "const now = new Date(new Date().toLocaleString(\"en-US\", {timeZone: \"Asia/Seoul\"}));\nconst yyyy = String(now.getFullYear());\nconst mm = String(now.getMonth() + 1).padStart(2, \"0\");\nconst dd = String(now.getDate()).padStart(2, \"0\");\nconst ymd = yyyy + mm + dd;\nconst dateLabel = `${yyyy}-${mm}-${dd}`;\nreturn [{ json: { yyyy, mm, dd, ymd, dateLabel } }];"
      }
    },
    {
      "id": "readfiles",
      "name": "오늘 리포트 파일 읽기",
      "type": "n8n-nodes-base.readWriteFile",
      "typeVersion": 1,
      "position": [420, 0],
      "parameters": {
        "operation": "read",
        "fileSelector": "={{ \"/root/obsidian/3protv/\" + $json.yyyy + \"/\" + $json.mm + \"/*\" + $json.ymd + \"*.md\" }}",
        "options": {}
      },
      "alwaysOutputData": true
    },
    {
      "id": "buildprompt",
      "name": "다이제스트용 본문 구성",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [640, 0],
      "parameters": {
        "jsCode": "// 3protv기사_*.md(원문 기사 덤프, 30~50KB)는 제외한다 — 이미 요약된\n// 오늘/정오/야간/ETF 리포트만 다이제스트 재료로 쓴다.\nconst items = $input.all();\nconst reports = [];\nconst indexMap = new Map();   // 지수명 → \"종가 아이콘 ▲/▼N%\"\nlet flowBlock = '';           // 수급 요약(개인/외국인/기관) 원문 블록\n\nconst INDEX_NAMES = ['다우존스', 'S&P500', '나스닥', 'KOSPI', '코스피', 'KOSDAQ', '코스닥'];\n// 2026-08-13 실물 리포트로 정정: report.py가 텔레그램용으로는 '•'를 쓰지만\n// 옵시디안 아카이브본은 표준 마크다운 '*   이름: 12,345.6 📈 ▲1.23%' 불릿이다.\n// 처음엔 '•' 기준으로 짰다가 실제 파일을 열어보고 나서야 이 차이를 발견했다 —\n// 함수 docstring만 보고 최종 렌더링 형식을 가정하지 말 것(교훈).\nconst QUOTE_RE = /^\\*\\s+([^:：\\n]+?)[:：]\\s*[\\d,.]+\\s*[^\\n]*?[▲▼]\\s*[\\d.]+%/gm;\n// 2026-08-14 실물 리포트로 재검증: report.py 프롬프트의 '수급주체 요약'은\n// LLM에게 주는 지시문 표현일 뿐, 실제 출력엔 그 문구가 안 박힌다 — 실제로는\n// '## 5) 🔎 수급 · 변동성' 헤딩 바로 아래 개인/외국인/기관합계 3줄이 온다.\n// 이전 정규식은 실물 파일 대신 프롬프트 문구를 보고 짜서 항상 매치 실패 →\n// 다이제스트에 '데이터 없음'만 떴다(같은 실수의 재발 — 지수 쪽과 동일 교훈).\nconst FLOW_RE = /^#{1,6}\\s*5\\)[^\\n]*수급[^\\n]*\\n((?:\\*\\s+(?:개인|외국인|기관합계)[:：][^\\n]*\\n?){1,3})/m;\n\nfor (const item of items) {\n  const bin = item.binary && item.binary.data;\n  if (!bin) continue;\n  const fileName = bin.fileName || '';\n  if (fileName.startsWith('3protv기사')) continue;\n  const buf = Buffer.from(bin.data, 'base64');\n  const text = buf.toString('utf-8');\n\n  // 지수·수급 줄은 잘리기 전 원문 전체에서 정규식으로 뽑는다 — 리포트 뒷부분에\n  // 있어도(수급 섹션이 특히 그렇다) 4000자 truncation과 무관하게 항상 잡힌다.\n  // LLM이 \"찾아서 정리\"하게 하는 대신 코드로 직접 추출 — 금액·등락률은\n  // 사람 손 없이 정확해야 하는 값이라 Gemini에 맡기지 않는다(2026-08-13 확정).\n  let m;\n  QUOTE_RE.lastIndex = 0;\n  while ((m = QUOTE_RE.exec(text)) !== null) {\n    const name = m[1].trim();\n    const hit = INDEX_NAMES.find(n => name.includes(n));\n    // 2026-08-14 실물 확인: 같은 지수가 '2) 주요 지표'(순수 지수 줄)와\n    // '4) 관심종목 업데이트'(🎤 마커 붙은 감시목록 줄)에 중복으로 나온다 —\n    // Map.set을 무조건 덮어쓰면 4)가 2)를 나중에 이겨 마이크 이모지가 섞인\n    // 줄이 최종값이 됐다(나스닥/S&P500 실측). 먼저 잡힌 값(2번 섹션이 항상\n    // 먼저 온다)을 유지해 순수 지수 줄을 우선한다.\n    if (hit && !indexMap.has(hit)) indexMap.set(hit, m[0].replace(/^\\*\\s+/, ''));\n  }\n  if (!flowBlock) {\n    const fm = text.match(FLOW_RE);\n    if (fm) flowBlock = fm[0].trim();\n  }\n\n  // Gemini에 넘길 본문은 여전히 4000자로 자른다 (흐름 요약·코스피 선물용 —\n  // 위치가 안 정해져 있어 코드 추출이 안 되고 LLM이 필요하다)\n  reports.push({ fileName, text: text.slice(0, 4000) });\n}\n\nif (reports.length === 0) {\n  return [{ json: { skip: true } }];\n}\n\nconst { ymd, dateLabel } = $('오늘 경로 계산').first().json;\nconst body = reports.map(r => `### ${r.fileName}\\n${r.text}`).join('\\n\\n');\n// line은 이미 정규식이 원문에서 그대로 뽑은 줄이라 이름을 포함한다\n// ('다우존스: 53839.99 📈 ▲0.13%') — 앞에 name을 또 붙이면 중복 노출된다\n// (2026-08-14 실측: '다우존스: 다우존스: 53839.99...'). line만 쓴다.\nconst indexLines = [...indexMap.entries()].map(([, line]) => line);\nconst indexSectionText = indexLines.length ? indexLines.join('\\n') : '데이터 없음';\nconst flowSectionText = flowBlock || '데이터 없음';\n\n// '리포트 언급 종목' 섹션은 2026-08-13 제거 — Gemini가 리포트에 실제로 없는\n// 회사명까지 만들어내는 걸 확인해 신뢰할 수 없다고 판단(사용자 확정).\nconst prompt = `아래는 ${dateLabel} 삼프로TV 관련 리포트 ${reports.length}건이다. `\n  + `다음만 처리해줘 (지수·수급은 이미 코드로 따로 추출했으니 다루지 않는다).\\n\\n`\n  + `1) 🌙 코스피 선물 (전일 대비)\\n`\n  + `\"코스피 선물\", \"코스피 야간선물\", \"코스피200 선물\" 관련 언급을 리포트에서 찾아 `\n  + `그 등락(수치가 있으면 수치, 방향만 있으면 방향만)을 한 줄로 써줘. 코스피 선물은 `\n  + `방송 화면을 읽어 적은 값이라 매일 나온다는 보장이 없다 — 언급이 아예 없으면 `\n  + `\"데이터 없음\"이라고만 쓰고, 절대 숫자를 추정해서 만들어내지 마.\\n\\n`\n  + `2) 📋 오늘 흐름 요약\\n`\n  + `핵심만 3~5줄 불릿으로 종합 요약해줘. 리포트별로 나열하지 말고 하루 흐름을 하나로 엮어서. `\n  + `리포트에 실제로 언급되지 않은 회사·수치는 절대 만들어내지 마.\\n\\n`\n  + `공통 규칙: 매매 시점·수량 등 개인화된 투자 자문은 하지 말고 정보 전달만 해줘.\\n\\n${body}`;\n\nreturn [{ json: {\n  skip: false, prompt, ymd, dateLabel, count: reports.length,\n  fileNames: reports.map(r => r.fileName),\n  indexSectionText, flowSectionText\n} }];"
      }
    },
    {
      "id": "if-skip",
      "name": "리포트 없음?",
      "type": "n8n-nodes-base.if",
      "typeVersion": 2.2,
      "position": [860, 0],
      "parameters": {
        "conditions": {
          "options": {
            "caseSensitive": true,
            "leftValue": "",
            "typeValidation": "loose",
            "version": 2
          },
          "conditions": [
            {
              "id": "skip-flag",
              "leftValue": "={{ $json.skip }}",
              "rightValue": "",
              "operator": {
                "type": "boolean",
                "operation": "true",
                "singleValue": true
              }
            }
          ],
          "combinator": "and"
        },
        "options": {}
      }
    },
    {
      "id": "noop-skip",
      "name": "종료(오늘 리포트 없음)",
      "type": "n8n-nodes-base.noOp",
      "typeVersion": 1,
      "position": [1080, -160],
      "parameters": {}
    },
    {
      "id": "gemini",
      "name": "Gemini 요약",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [1080, 100],
      "parameters": {
        "method": "POST",
        "url": "https://generativelanguage.googleapis.com/v1beta/models/gemini-3.1-flash-lite:generateContent?key=<N8N_GEMINI_API_KEY>",
        "sendBody": true,
        "specifyBody": "json",
        "jsonBody": "={{ JSON.stringify({ contents: [{ parts: [{ text: $json.prompt }] }] }) }}",
        "options": {
          "response": {
            "response": {
              "neverError": true
            }
          }
        }
      }
    },
    {
      "id": "parseresult",
      "name": "요약 파싱",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [1280, 100],
      "parameters": {
        "jsCode": "// Gemini 실패(429 할당량 소진 등)도 조용히 죽지 않고 텔레그램으로 알린다\n// (2026-07-27 GH_PAT 침묵 실패 사고 재발 방지 원칙과 동일선상)\nconst res = $json;\nconst built = $('다이제스트용 본문 구성').first().json;\nconst { yyyy, mm } = $('오늘 경로 계산').first().json;\nconst candidate = res.candidates && res.candidates[0];\nconst text = candidate && candidate.content && candidate.content.parts && candidate.content.parts[0]\n  ? candidate.content.parts[0].text.trim()\n  : null;\n\n// 지수·수급은 Gemini와 무관하게 코드로 이미 추출돼 있다 — Gemini가 실패해도\n// 이 두 섹션은 그대로 살아서 나간다(2026-08-13: 숫자 데이터는 요약 실패와\n// 별개로 최대한 전달하자는 원칙).\nconst dataBlock = `📊 주요 지수 (전일 대비)\\n${built.indexSectionText}\\n\\n`\n  + `💰 수급 동향 (개인/외국인/기관, 전일대비 · 억원)\\n${built.flowSectionText}\\n\\n`;\n\nlet message;\nif (text) {\n  message = `📋 3프로TV 오늘의 종합 다이제스트 (${built.dateLabel})\\n\\n${dataBlock}${text}\\n\\n`\n    + `— 오늘 발행된 리포트 ${built.count}건 종합 (원문: 옵시디안 3protv/${yyyy}/${mm}/)`;\n} else {\n  message = `⚠️ 오늘(${built.dateLabel}) 다이제스트 요약 생성 실패 (Gemini 응답 이상 — 할당량 소진 가능성)\\n`\n    + `개별 리포트 ${built.count}건은 정상 저장됐습니다 — 옵시디안에서 직접 확인해주세요.\\n\\n${dataBlock}`;\n}\n\n// 텔레그램 chat_id는 콤마로 합쳐서 한 번에 못 보낸다(2026-08-13 사고 —\n// Bot API는 요청 1회당 chat_id 1개만 받는다). 방마다 아이템을 나눠 반환하면\n// 다음 노드(텔레그램 발송)가 아이템 수만큼 자동으로 반복 호출된다.\nconst chatIds = [\"<N8N_TELEGRAM_CHAT_ID_1>\", \"<N8N_TELEGRAM_CHAT_ID_2>\"];\nreturn chatIds.map(id => ({ json: { text: message, chat_id: id } }));"
      }
    },
    {
      "id": "tg-send",
      "name": "텔레그램 발송",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [1480, 100],
      "parameters": {
        "method": "POST",
        "url": "https://api.telegram.org/bot<N8N_TELEGRAM_BOT_TOKEN>/sendMessage",
        "sendBody": true,
        "specifyBody": "json",
        "jsonBody": "={{ JSON.stringify({ chat_id: $json.chat_id, text: $json.text }) }}",
        "options": {}
      }
    }
  ],
  "connections": {
    "평일 18:00": {
      "main": [[{ "node": "오늘 경로 계산", "type": "main", "index": 0 }]]
    },
    "오늘 경로 계산": {
      "main": [[{ "node": "오늘 리포트 파일 읽기", "type": "main", "index": 0 }]]
    },
    "오늘 리포트 파일 읽기": {
      "main": [[{ "node": "다이제스트용 본문 구성", "type": "main", "index": 0 }]]
    },
    "다이제스트용 본문 구성": {
      "main": [[{ "node": "리포트 없음?", "type": "main", "index": 0 }]]
    },
    "리포트 없음?": {
      "main": [
        [{ "node": "종료(오늘 리포트 없음)", "type": "main", "index": 0 }],
        [{ "node": "Gemini 요약", "type": "main", "index": 0 }]
      ]
    },
    "Gemini 요약": {
      "main": [[{ "node": "요약 파싱", "type": "main", "index": 0 }]]
    },
    "요약 파싱": {
      "main": [[{ "node": "텔레그램 발송", "type": "main", "index": 0 }]]
    }
  }
}
